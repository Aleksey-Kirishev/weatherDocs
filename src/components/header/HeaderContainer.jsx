/*
import React from "react"
import {connect} from "react-redux";
import "./Header.module.css"
import Header from "./Header";
import {getWeatherData, saveValue} from "../../redux/content_reducer";

function HeaderContainer(props) {
    return <header>
        <Header name={props.name} cities={props.cities} getWeatherData={props.getWeatherData} weather={props.weather}
                city={props.city} saveCity={props.saveCity}/>
    </header>
}

*/

